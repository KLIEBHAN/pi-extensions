Create a minimal Hello World deliverable in `/app`.

Requirements:

1. Create `/app/hello.txt` containing exactly this single line, including the trailing newline:

   ```text
   Hello, world!
   ```

2. Create `/app/hello.sh` as an executable shell script that prints exactly this single line to stdout, including the trailing newline:

   ```text
   Hello, world!
   ```

Leave both files in place when you finish.
